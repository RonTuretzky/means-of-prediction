# Evidence basis diagnostic

This private procedure compares the unchanged baseline judge with a variant that
records an exact source quotation, entity, quantity and short comparison before
the original five output fields. It uses the same twelve selected development
examples as the earlier order and split-predicate probes. It does not measure
independent accuracy or population settlement coverage.

The panel, complete input bytes, baseline rules, source closure and runtime were
frozen before V3 local inference. The reporting amendment preserves the original
sources and additionally binds failed/capped responses to their raw metadata.
Sixteen synthetic tests pass. The child coordinator reviewed the procedure and
amendment. No inference has run at this preparation checkpoint.

Execute only after V3 has finished and the root coordinator verifies exclusive
local-model access. Write the one-time `run-approved.json` with the verified
input-freeze, reviewed, completed V2 and completed V3 judgment hashes, actual
timestamp, and `localInferenceQuiescent: true`. Then run `python3 probe.py preflight`
and `python3 probe.py run` from this original private directory. Never blindly
repeat these commands after interruption: reconcile started/result artifacts
first. The run performs 24 calls and writes its raw-bound summary automatically.

The added evidence basis never repairs or overrides final predictions. The
original evidenceQuote alone supplies the existing exact-quote scoring credit;
basis quotation quality is a separate diagnostic. Missing usage remains unknown.
Review all twelve extracted bases against their full evidence before considering
another method. A useful result still requires a named full-cohort method with
request/schema/scoring lineage and reserved-evaluation adapter review.

Any source copies under the Git repository are archival. Their relative roots
point elsewhere if executed there. Continue from this original private directory;
keep emails, requests, results and labels out of public Git history.
